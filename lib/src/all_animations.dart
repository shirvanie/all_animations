

import 'package:flutter/material.dart';

class Animate extends StatelessWidget {
  const Animate({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("All Animations"),);
  }
}
